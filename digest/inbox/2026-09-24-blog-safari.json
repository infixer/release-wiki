{
  "kind": "blog",
  "id": "safari",
  "title": "Safari リリースノート",
  "relatedRepo": null,
  "collectedAt": "2026-09-24T08:29:58Z",
  "posts": [
    {
      "title": "Safari 26.4 Release Notes",
      "url": "https://developer.apple.com/documentation/safari-release-notes/safari-26_4-release-notes",
      "publishedAt": "2026-03-24T00:00:00.000Z",
      "versions": [
        "26.4"
      ],
      "text": "## Overview\n\nSafari 26.4 is available for iOS 26.4, iPadOS 26.4, visionOS 26.4, macOS 26.4, macOS Sequoia, and macOS Sonoma.\n\n### Accessibility\n\n#### Resolved Issues\n\n- Fixed an issue where Voice Control commands could cause Safari to hang.\n- Fixed an issue where a button’s label would not update when a descendant’s `aria-hidden` attribute changed.\n- Fixed elements with `aria-controls` or `aria-expanded` and the `hidden` attribute to no longer appear in VoiceOver’s Form Control menu.\n- Fixed an issue where VoiceOver would announce with extra verbosity when moving onto the first item of a list on the webpage. \n- Fixed an issue where controls with `aria-labelledby` pointing to visually-hidden elements could not obtain proper bounding box geometry through accessibility APIs.\n\n### Animations\n\n#### New Features\n\n- Added support for threaded scroll-driven animations, improving performance.\n\n#### Resolved Issues\n\n- Fixed incorrect overlap calculations for transform animations including `translate`, `scale`, and `rotate` properties.\n\n### Browser\n\n#### New Features\n\n- Added Compact tabs back to Safari on macOS and iPadOS. \n\n#### Resolved Issues\n\n- Fixed Safari gesture support to prevent pages that should not be able to scroll, such as with explicit `overflow: hidden`, from unexpectedly scrolling. \n\n### CSS\n\n#### New Features\n\n- Added support for a new blocks-in-inline layout approach.\n- Updated absolutely positioned boxes in scrollable containing blocks to allow alignment overflow in scrollable directions, in accordance with a recent CSS spec change.\n- Added support for `flow-tolerance` in CSS Grid Lanes.\n- Added support for a new grid layout integration system resolving a number of grid layout issues.\n- Added support for parsing and evaluating name-only `@container` queries that have no conditions, allowing named containers to match without explicit constraints.\n- Added support for an automatic initial value for `grid-auto-flow` in CSS Grid Level 3 to switch flow orientation based on `grid-template-rows` and `grid-template-columns` for grid lane layouts.\n- Added support for CSS `display: grid-lanes`.\n- Added support for `math-depth`.\n- Added support for the `cursor` property on the `::marker` pseudo-element.\n\n#### Resolved Issues\n\n- Fixed an issue where `display: list-item` was incorrectly supported on `fieldset`.\n- Fixed an issue where `max-width` was not correctly applied to tables with fixed widths.\n- Fixed incorrect sizing and fragment URL handling for SVG images used in `-webkit-cross-fade()`.\n- Fixed `text-combine-upright` to properly ignore `letter-spacing` when composing text horizontally, aligning with the CSS Writing Modes specification.\n- Fixed an issue where `background-blend-mode` was not applied correctly when combined with `background-clip: text`.\n- Fixed table layout so that fixed horizontal margins on `<caption>` elements now contribute to the table’s minimum preferred logical width, preventing captions from causing narrower than expected tables.\n- Fixed incorrect width calculation for `positioned` elements using `box-sizing: border-box` with an `aspect-ratio`, ensuring borders and padding are not double-counted.\n- Fixed the UA style sheet to use `:focus-visible` instead of `:focus` for outline properties.\n- Fixed `HighlightRegistry` to remove its non-standard `constructor` and updated tests to use `CSS.highlights` while ensuring `Map.prototype` is properly restored after tampering.\n- Fixed handling of `@property` registration so that the `initial-value` descriptor can be optional.\n- Fixed baseline alignment for grid items by adding correct first baseline and last baseline row axis handling and properly accounting for baseline offsets. \n- Fixed baseline handling for table cells when cell has no inflow children.\n- Fixed `getComputedStyle(\"top\")` to correctly resolve percentage values for absolutely positioned elements inside inline containers.\n- Fixed an infinite style resolution loop when a `position-try` box was inside a `display: none` subtree.\n- Fixed `width`, `height`, `min-width`, `min-height`, `max-width` and `max-height` to apply CSS zoom at used-value time.\n- Fixed CSS zoom to scale `<iframe>` element contents.\n- Fixed `getBoundingClientRect` and `getClientRects` to return scaled lengths according to CSS zoom instead of unscaled values, aligning with the CSS Viewport specification.\n- Fixed scrolling behavior so that `scrollRectToVisible()` can bring fixed anchor-positioned boxes outside the viewport into view, improving keyboard navigation.\n- Fixed an issue where `@font-face` and `FontFace.family` failed when the font family name contained spaces, ensuring the family name is now treated as a plain string instead of being parsed.\n- Fixed `top`, `left`, `right`, and `bottom` to apply CSS zoom at used-value time\n- Fixed `margin` to apply CSS zoom at used-value time.\n- Fixed evaluation of `calc()` expressions to correctly apply the used `zoom` factor to length values, ensuring properties like `line-height` and box dimensions scale properly.\n- Fixed an issue where `calc(em)` values for unzoomed properties were incorrectly adjusted.\n- Fixed `position-area` normal alignment to align toward the non-auto inset when only one inset is auto, in accordance with recent CSS spec changes.\n- Fixed an issue where `calc()`  incorrectly treated margins as present.\n- Fixed `devicePixelRatio` so that page zoom now affects the main frame consistently with iframes, keeping their values synchronized.\n- Fixed incorrect underline positioning for `text-decoration` when inline box sides are trimmed.\n- Fixed an issue where CSS `@starting-style` entry animations were only applied on the first transition, especially when interacting with anchor positioning or position fallbacks.\n- Fixed an issue where fixed-positioned boxes using `position-area` were incorrectly included in the root scrollable containing block, ensuring they position correctly while still allowing overflow when appropriate.\n- Fixed `-webkit-line-clamp` so that it no longer propagates into `inline-block` children.\n- Fixed `line-height` to correctly scale font-relative units when text zoom is applied.\n- Fixed an issue where nested identical CSS `filter` effects were not rendered.\n- Fixed `element.clientWidth` and `element.clientHeight` to correctly include padding for `content-box` tables.\n- Fixed: Refactored the handling of block-level boxes inside inline boxes.\n- Fixed table column width distribution when a `colspan` spans mixed percentage and auto-width columns to properly respect percentage constraints.\n- Fixed an issue where `text-decoration: underline` appeared higher than expected when `text-box-trim` was applied to the root inline box.\n- Fixed `::first-line` pseudo-element to always use `inline` display to match the CSS Display specification.\n- Fixed an issue where `shape-outside` did not update correctly after web fonts loaded.\n- Fixed incorrect text selection ranges for truncated right-to-left content.\n- Fixed table height calculation to correctly account for captions with orthogonal `writing-mode`.\n- Fixed `counter-*` properties serialization order.\n- Fixed `outline-width` and `outline-offset` to follow updated computed style resolution rules.\n- Fixed the computed style resolution for `border-*-width` properties.\n- Fixed performance and correctness issues with inheritance in the modern CSS Zoom implementation.\n- Fixed the computed style resolution for the `column-rule-width` property.\n- Fixed `border-*-width`, `outline-width`, and `column-rule-width` so they now pixel snap correctly during CSS animations and transitions.\n- Fixed the disclosure triangle in `<details>` elements to use the larger `system-ui` font variant, preventing it from rendering as an emoji and ensuring consistent display across writing directions.\n- Fixed CSS rules within `@scope` not being applied to `<input>` and `<textarea>` elements.\n- Fixed an issue where empty CSS rules sharing a selector with non-empty rules could prevent dynamic style updates from applying correctly.\n- Fixed an issue where `padding-inline-end` was not included in the scrollable overflow for block containers.\n- Fixed an issue where `position-area` for elements in scrollable containers only used the initial scrollport bounds instead of the entire scrollable area.\n- Fixed an issue where CSS transitions were incorrectly triggered when `border-width` computed values did not change, causing unexpected layout shifts.\n- Fixed an issue where absolutely-positioned elements inside inline containers did not correctly account for the margin of a preceding block when determining their static position.\n- Fixed an issue where multi-column layout rendered columns at incorrect positions for some fonts.\n\n#### Deprecations\n\n- Removed the `FontFaceSet` constructor from the CSS Font Loading API as it was deemed unnecessary, aligning with the CSSWG resolution.\n\n### Canvas\n\n#### Resolved Issues\n\n- Fixed `ImageBitmap` created from SVG image sources to correctly honor the `flipY` orientation.\n\n### Clipboard\n\n#### Resolved Issues\n\n- Fixed an issue where using the “Copy Image” context menu in Safari would also copy the image URL, causing some sites to paste the URL instead of the image.\n\n### Editing\n\n#### Resolved Issues\n\n- Fixed incorrect text selection when dragging across pseudo elements.\n- Fixed an issue on iOS 26 where the edit menu would not appear when tapping inside an already-focused text field that had a looping `content` animation.\n- Fixed an issue where focusing a hidden editable element would incorrectly display the text cursor and selection at full opacity.\n- Fixed a regression where dragging to select text would stop scrolling when the cursor left the window.\n\n### Encoding\n\n#### Resolved Issues\n\n- Fixed incorrect handling of invalid UTF-8 sequences in the `TextDecoder` streaming decoder to properly manage partial sequence buffers.\n\n### Events\n\n#### Resolved Issues\n\n- Fixed an issue where `mouseleave` and `mouseout` events were not dispatched when a window moved out from under a stationary cursor.\n\n### Forms\n\n#### Resolved Issues\n\n- Fixed an issue where input fields did not display user input while typing.\n- Fixed an issue where `input[type=\"search\"]` fields with `appearance: none` incorrectly reserved space for the datalist dropdown button.\n- Fixed an incorrect fallback for the menu style for empty lists, improving readability and correctness.\n\n### HTML\n\n#### New Features\n\n- Added support for using the `min()`, `max()`, and `clamp()` math functions in the `sizes` attribute of `<img>` elements.\n\n#### Resolved Issues\n\n- Fixed an issue where nested `about:blank` frames were incorrectly treated as self-referencing, preventing them from loading.\n- Fixed `shadowrootcustomelementregistry` attribute serialization to correctly compare ShadowRoot and document registries.\n- Fixed `HTMLImageElement.currentSrc` to return an empty string for `<img src=\"\">` instead of resolving to the document base URL.\n\n### Images\n\n#### Resolved Issues\n\n- Fixed image uploading to not transcode images when `accept=\"image/*\"` is specified.\n\n### JavaScript\n\n#### New Features\n\n- Added support for iterator sequencing. \n\n#### Resolved Issues\n\n- Fixed `Date` constructor overflow handling so that invalid day values now return `NaN`.\n- Fixed `Intl.DateTimeFormat` to throw a `RangeError` for legacy non-IANA timezones, aligning behavior with TC39 standards.\n- Fixed `Intl.Locale.prototype.getWeekInfo()` to remove the `minimalDays` property for compliance with the specification.\n- Fixed `Intl.NumberFormat` to properly apply `minimumFractionDigits` and `maximumFractionDigits` to ensure currency and compact notations behave correctly.\n- Fixed `%TypedArray%.prototype.includes` to correctly check that the `index` is less than the array length, aligning its behavior with ECMA-262.\n- Fixed async functions without any `await` to inline their bodies for performance and corrected async stack traces with a single function entry for exceptions thrown from or through async functions.\n\n### MathML\n\n#### New Features\n\n- Added support for CSS size containment in MathML elements using `contain-intrinsic-inline-size` and `contain-intrinsic-block-size`.\n- Added support for `math-style` and `math-shift` to animate as discrete values.\n\n#### Resolved Issues\n\n- Fixed default MathML rule thickness to use the font’s `underlineThickness` metric with a zero fallback.\n- Fixed `mpadded` elements in RTL (`dir=\"rtl\"`) to respect `lspace`.\n- Fixed an issue where empty `<msqrt>` radical operators in MathML were not painted correctly.\n- Fixed MathML `<mpadded>`, `<mfrac>`, `<munderover>`, `<mover>` and `<mspace>` elements not updating layout when attributes like `width`, `height`, `depth`, `lspace`, and `voffset` etc. changed.\n- Fixed MathML boolean attributes so they are now compared ASCII case-insensitively.\n- Fixed incorrect positioning of `mpadded` content in right-to-left mode.\n- Fixed an issue where `<msqrt>` or `<mroot>` elements did not update their radical operators when children were dynamically added or removed.\n- Fixed `<mpadded>` so that percentage values for `width`, `height`, and `depth` attributes are treated as absent and use content dimensions as defaults, matching the MathML Core specification.\n\n### Media\n\n#### New Features\n\n- Added support for a pop-up menu on macOS that allows users to select and manage caption style profiles.\n\n#### Resolved Issues\n\n- Fixed an issue where `<video>` poster images were incorrectly double-scaled when `zoom` was applied by using the cached intrinsic poster size without reapplying zoom.\n- Fixed dispatching of enter and exit events on `TextTrackCue` and `VTTCue` objects with no associated track, aligning with other browsers.\n- Fixed an issue where changing an `HTMLMediaElement` volume from `0` to `0` did not activate the audio session or update the sleep disabler.\n- Fixed an issue where videos would freeze on the first frame when transitioning from encrypted to clear content by ensuring the decoded buffer maintains at least 100ms of frames even when the next frame is far in the future.\n- Fixed an issue where the mute button disappeared in macOS inline videos with adjustable sizes.\n- Fixed playback of `application/ogg` blob media.\n- Fixed an issue where Video Viewer UI elements overlapped or exited unexpectedly.\n- Fixed an issue where empty `<track>` elements prevented media from advancing its readyState and blocked `play()` calls.\n- Fixed an issue where `HTMLMediaElement` did not correctly detect new audio or video tracks causing Safari to pause video when leaving a tab.\n- Fixed a crash in `AudioData.copyTo()` when copying the last channel of 3-channel audio.\n- Fixed an issue where the `ended` event for Media Source Extensions might never fire by ensuring buffered ranges update correctly and playback gaps are observed even when the video does not start at time zero.\n- Fixed an issue where caption previews were not shown in the default media controls.\n- Fixed the caption menu’s `On` option to correctly enable the highest-scoring text track and mark the appropriate language as checked in the subtitle menu.\n- Fixed `parseSequenceHeaderOBU` to return an `AV1CodecConfigurationRecord`, fully decode the Sequence Header OBU, and capture the complete color profile.\n- Fixed an issue where the macOS inline media controls timeline scrubber overlapped the right container buttons.\n- Fixed an issue where `WebCodecs VideoDecoder` could output H264 frames in the wrong order.\n- Fixed an issue where the mute button and volume slider overlapped in the video player controls when using a right-to-left language.\n\n### Networking\n\n#### New Features\n\n- Added support for WebTransport. \n\n#### Resolved Issues\n\n- Fixed a regression where `fetch()` would throw a `TypeError` when using `targetAddressSpace: 'loopback'` for localhost requests.\n\n### Privacy\n\n#### Resolved Issues\n\n- Fixed an issue where cross-browser Private Click Measurement recorded attribution entries even when “Allow privacy-preserving measurement of ad effectiveness” was disabled. \n\n### Rendering\n\n#### Resolved Issues\n\n- Fixed over-aggressive clipping of child layers in multicolumn layouts to prevent visual overflow issues with `position: relative` elements and `transform:scale()` text.\n- Fixed unreadable Scroll-to-Text-Fragment highlights on dark pages.\n- Fixed an issue where auto-positioned absolutely positioned descendants were not always marked for layout when their parent’s border box moved.\n- Fixed an issue where positioned, transformed, or opacity-altered `<img>` elements with HDR JPEG gainmaps would incorrectly render in SDR.\n- Fixed an issue on iPadOS where closing the sidebar or resizing the window could cause the page to remain zoomed in by recalculating the target scale to automatically match the new minimum scale.\n- Fixed an issue where fixed positioned elements were not rendered correctly in right-to-left pages using the `vertical-rl` writing mode.\n- Fixed a performance issue in  layouts with long `pre` blocks and `word-break: break-all` by including whitespace in overflow width calculations.\n- Fixed an issue where overconstrained sticky elements were not properly adjusting their insets when the sticky box rectangle was larger than the viewport.\n- Fixed an issue where applying `word-spacing` to `::first-line` could cause other lines to disappear.\n- Fixed inconsistent text layout when using `list-style-type` by ensuring outside list markers do not affect intrinsic width calculations.\n- Fixed incorrect min and max width calculations for block-level boxes inside inline content.\n- Fixed an issue where color fonts could affect the color of other DOM elements.\n- Fixed an issue where `getClientRects` returned an incomplete list of rectangles for inline boxes containing block elements.\n- Fixed `<col>` elements with `span > 1` not applying their width to all spanned columns during table layout, aligning behavior with other browsers.\n- Fixed table layout `min-width` distribution for spanning cells with mixed `percent`, `fixed`, and `auto` columns.\n- Fixed: Improved drop-shadow and blur effects rendering performance.\n\n### SVG\n\n#### New Features\n\n- Added support for the `lighter` operator in `SVGFECompositeElement` IDL to align with the Compositing and Blending specification.\n\n#### Resolved Issues\n\n- Fixed an issue where `stroke-dasharray` incorrectly propagated to SVG markers when explicitly marked as ‘0’.\n- Fixed an issue where `foreignObject` elements in SVG incorrectly allowed margin collapsing.\n- Fixed SVG intrinsic sizing and `preferredAspectRatio()` to correctly transpose dimensions for vertical writing modes.\n- Fixed animation of the `stop-color` attribute on `<stop>` elements.\n- Fixed an issue where dynamically changing `marker-start`, `marker-mid`, or `marker-end` attributes on SVG elements did not trigger re-rendering.\n- Fixed tiling gaps in CSS reference filters using `<feDisplacementMap>`.\n- Fixed `SVGLength` parsing to correctly return the initial value when encountering parser errors or invalid values.\n- Fixed an issue where `SVGImage` did not respect system dark mode changes.\n- Fixed breaking SVG resource referencing when removing a resource which shares its `id` with other resources.\n- Fixed behavior to avoid incorrect pruning of SVG mask subtrees based on visibility.\n- Fixed an issue where SVG `animateTransform` animations on hidden elements were triggering full-page rendering updates each frame.\n- Fixed `SVGLength.prototype.valueAsString` to throw a `SyntaxError` when assigned an empty string.\n- Fixed `SVGLength` percentage resolution for elements inside non-instanced `<symbol>` elements.\n- Fixed an issue where lengths with leading or trailing whitespace failed to be parsed.\n- Fixed an issue where `SVGLength.value` did not update for font-relative units (e.g., `ch`, `em`) after changes to `writing-mode`.\n- Fixed missing gradient fills when using paint-order `stroke fill`.\n- Fixed embedded `<svg>` elements in `<img>` without an explicit `viewBox` to synthesize `preserveAspectRatio='none'` so the SVG stretches to fill the container.\n- Fixed the `<stop>` element `offset` attribute in SVG to reject invalid values with trailing characters and correctly fall back to `0`.\n- Fixed the transform order for `clipPath` elements so that the bounding box is scaled correctly before applying the local transform.\n- Fixed `<clipPath>` to clip to its `<use>` child element based on the visibility of the `<use>` target element.\n- Fixed incorrect rendering when combining `markerUnits=strokeWidth` with `vector-effect=non-scaling-stroke`.\n- Fixed displaying an SVG filter referencing an element with a huge stroke.\n- Fixed hit testing for overlapping `<text>` and `<tspan>` elements in SVG.\n- Fixed rendering of dimensionless SVG images.\n\n### Spatial Web\n\n#### Resolved Issues\n\n- Fixed auto-dimming for playback sessions in visionOS.\n\n### Storage\n\n#### Resolved Issues\n\n- Fixed an issue where IndexedDB databases might have mismatched metadata version and database name encoding format.\n\n### Text\n\n#### New Features\n\n- Added support for parsing and computing `font-size: math` with scaling logic.\n\n### WKWebView\n\n#### Resolved Issues\n\n- Fixed an issue where `WKWebView` apps with a toolbar would fail to display a top scroll edge effect when relying on automatic content inset adjustments.\n\n### Web API\n\n#### New Features\n\n- Added support for `ReadableStream.getIterator()` and the `[@@asyncIterator]` methods to enable iteration over streams.\n- Added support for auxiliary mouse button values in `MouseEvent.button`.\n- Added support for the Keyboard Lock API.\n- Added support for using readable byte streams as `fetch` request and response bodies and enabling synchronous start behavior.\n- Added support for reading `Blob.stream()` with a BYOB (`getReader({mode:'byob'})`) reader.\n- Added support for `ReadableByteStream`.\n- Added support for upgrading elements in `CustomElementRegistry.prototype.initialize`.\n- Added support for the `customelementregistry` content attribute and handling of `null` `customElementRegistry` values in `document.createElement`, `document.createElementNS`, and `element.attachShadow`.\n- Exposed the`MediaDeviceInfo` interface  in secure contexts only. per the web specification.\n- Added support for Resource Timing Level 3 attributes `finalResponseHeadersStart` and `firstInterimResponseStart` to capture timing for both interim (1xx) and final HTTP response headers for proper measurement of events like 103 Early Hints.\n\n#### Resolved Issues\n\n- Fixed `DeviceMotionEvent` and `DeviceOrientationEvent` interfaces so that they only show up in secure contexts just like the corresponding events and made `ondevicemotion` and `ondeviceorientation` enumerable, aligning with the specification.\n- Fixed an issue where `IntersectionObserver` computed the root rectangle incorrectly when overflow clipping was present.\n- Fixed Trusted Types to correctly send CSP violation reports when a default policy returns an invalid `javascript:` URL or throws an exception.\n- Fixed `Element.requestFullscreen` on iOS to correctly reflect hardware keyboard attachment state during fullscreen sessions and exit automatically when the keyboard is detached.\n- Fixed event ordering and committed promise timing for intercepted `Navigation API` traverse navigations.\n- Fixed the processing order of Trusted Types for DOM attribute setting.\n- Fixed `NavigateEvent` to correctly fire an `AbortSignal` when a navigation is aborted.\n- Fixed `NavigateEvent.sourceElement` to correctly reference elements from different browsing contexts.\n- Fixed the Navigation API to prevent scripts from flooding the system with navigation requests.\n- Fixed an issue where `scroll-margin` from `IntersectionObserver` incorrectly applied to scrollers inside cross-origin iframes.\n- Fixed `ReadableStream` and `WritableStream` to correctly pass abort and cancel reasons and improved `WebTransport` stream handling.\n- Fixed `DigitalCredential` behavior to make user mediation implicitly required.\n- Fixed an issue where the Big5 `TextDecoder` failed to recover and emit ASCII characters after encountering an invalid leading byte.\n- Fixed `MouseEvent.offsetX` and `MouseEvent.offsetY` so they are now calculated relative to the padding edge of the target element.\n- Fixed an issue where pointer events would fail to dispatch after a parent view’s custom gesture recognizer blocked `touchesEnded:` or `touchesCancelled:`.\n- Fixed an issue where JavaScript-to-native object serialization could fail entirely when any individual key-value pair could not be serialized, causing broken workflows in some apps.\n\n### Web Authentication\n\n#### New Features\n\n- Added support for the WebAuthn PRF extension that maps to the CTAP `hmac-secret` extension, enabling credential-bound cryptographic secrets for both credential creation and authentication flows with security keys.\n- Added support for WebAuthn CTAP PIN/UV Auth Protocol 2 using HKDF-SHA-256 for key derivation, enabling proper FIPS-compliant authenticators.\n\n#### Resolved Issues\n\n- Fixed an issue where the `excludeCredentials` list was not sent over CTAP when its size was 1.\n\n### Web Extensions\n\n#### Resolved Issues\n\n- Fixed an issue where `window.open()` calls from web extensions would incorrectly open “about:blank” instead of the intended URL by ensuring each extension URL loads in a fresh tab configuration. \n\n### Web Inspector\n\n#### New Features\n\n- Added support for capturing `console.screenshot` images within a `Worker`, including handling of `ImageData`, `ImageBitmap`, `OffscreenCanvas`, various `CanvasRenderingContext` types, and valid base64 `data:` URLs.\n- Added support for starting and stopping `<canvas>` recordings from the `console` within a `Worker` using `console.record()` and `console.recordEnd()`.\n- Added a context menu option to DOM nodes in the Elements Tab to copy pretty-printed HTML.\n- Added a setting to toggle the visibility of User Agent stylesheet rules in the Elements tab.(164265183)\n- Added support for showing actual composited layer snapshots in the 3D Layers view.\n- Added support in the Web Inspector Timelines Heap view to display the dominator object, if any, when viewing the shortest GC path.\n- Added support for auto-completion of `sideways-lr` and `sideways-rl` values for the `writing-mode` CSS property.\n- Added support for auto-completion of `grid-lanes` and `inline-grid-lanes` values for the `display` CSS property.\n- Added support for Web Inspector to fetch CSS property keyword completions based on feature support.\n- Added support for displaying `Order Numbers` in CSS Grid and CSS Grid Lanes overlays in Web Inspector, including new UI settings for toggling order number visibility.\n- Added Grid Inspector support for visualizing gaps between items arranged by Grid Lanes.\n\n#### Resolved Issues\n\n- Fixed incorrect breakpoint and search result positions in the Web Inspector after pretty-printing inline scripts containing multi-line template literals.\n- Fixed the Styles sidebar filter in Web Inspector to be case-insensitive.\n- Fixed an issue where a large number of search results in the Search tab would freeze Web Inspector.\n- Fixed an issue where the Console tab search bar in Web Inspector would disappear when the window was too narrow.\n- Fixed an issue where CSS properties added to new rules were not applied and were marked as invalid.\n- Fixed context menu items to rename `Copy HTTP Request` and `Copy HTTP Response` to `Copy HTTP Request Headers` and `Copy HTTP Response Headers` for clarity.\n- Fixed incorrect grid track sizing display in the Web Inspector when using CSS custom properties.\n- Fixed an issue in the Console where the count of identical consecutive messages could be wrong.\n- Fixed an issue where breakpoints and search results in Web Inspector could point to the wrong location after a previously formatted source file was reopened in an unformatted state.\n- Fixed an issue where the Sources navigation sidebar could be empty when reloading the page.\n- Fixed timestamp formatting in the Web Inspector to remove unnecessary trailing `.0` values for readability.\n- Fixed item order labels in the Web Inspector grid and flex overlays to remove the `#` symbol, changing from `Item #N` to `Item N`.\n- Fixed an issue where the text filter in the Sources tab did not apply to the Local Overrides and Console Snippets sections.\n- Fixed a performance issue in the Web Inspector search panel by limiting initial results to 100 per resource and adding UI controls to load more or all results, reducing unnecessary UI work for large queries.\n- Fixed an issue where the search bar settings icon disappeared when the search field was focused.\n\n### WebAssembly\n\n#### Resolved Issues\n\n- Fixed `Error.isError(WebAssembly.Exception)` to correctly return `false` based on current WebAssembly spec semantics.\n\n### WebGPU\n\n#### Resolved Issues\n\n- Fixed incorrect handling of some PNG pixel formats in WebGPU.\n\n### WebRTC\n\n#### New Features\n\n- Added support for capturing audio from multiple microphones on macOS with `getUserMedia` while managing echo cancellation and dynamically migrating existing captures to non-VPIO units.\n- Added network slicing support for WebRTC on iOS. \n\n#### Resolved Issues\n\n- Fixed `RTCDataChannelInit` to support `[EnforceRange]` on the `maxPacketLifeTime` and `maxRetransmits` fields to align with the WebRTC specification.\n- Fixed an issue on macOS where calling `getUserMedia` with echo cancellation disabled could unintentionally affect existing audio tracks.\n- Fixed `MediaStreamTrackProcessor` to respect `track.enabled = false`.\n- Fixed an issue where `RTCDataChannel` close events did not fire when `RTCPeerConnection` was closed.\n- Fixed `RTCConfiguration.iceServers` to be a non-optional sequence with an empty array as the default, improving spec compliance and ensuring `RTCPeerConnection` behaves correctly when `iceServers` is undefined."
    },
    {
      "title": "Safari 26.5 Release Notes",
      "url": "https://developer.apple.com/documentation/safari-release-notes/safari-26_5-release-notes",
      "publishedAt": "2026-05-11T00:00:00.000Z",
      "versions": [
        "26.5"
      ],
      "text": "## Overview\n\nSafari 26.5 is available for iOS 26.5, iPadOS 26.5, visionOS 26.5, macOS 26.5, macOS Sequoia, and macOS Sonoma.\n\n### Accessibility\n\n#### Resolved Issues\n\n- Fixed an issue where the accessibility tree could permanently be empty if built during early page load when only a scroll area and web area were present.\n\n### Animations\n\n#### Resolved Issues\n\n- Fixed support for the `scroll` animation timeline range name in scroll-driven animations.\n- Fixed an issue where scroll-driven animations were not properly paused when `animation-play-state` was dynamically set to `paused`.\n- Fixed an issue where view timeline animations near the 0% and 100% thresholds reported incorrect progress values.\n- Fixed a regression where animating to an implicit value for individual transform properties failed to animate.\n- Fixed an issue where animation timelines could fail to restore correctly after navigating back to a page from the back-forward cache.\n\n### CSS\n\n#### New Features\n\n- Added support for the `:open` pseudo-class for `<details>`, `<dialog>`, `<select>`, and `<input>` elements.\n- Added support for `element-scoped` keyword in `random()`. Updated `random()` caching behavior so that custom property identifiers like `random(--foo)` are matched globally to the document rather than scoped to individual elements, aligning with the updated specification. Removed support for `element-shared`.\n\n#### Resolved Issues\n\n- Fixed an issue where a `display: grid` subgrid inside a `grid-lanes` container incorrectly contributed its items’ intrinsic sizes to the parent’s track sizing algorithm.\n- Fixed an issue in collapsed border tables where the border style of a cell adjacent to a `rowspan` cell was incorrectly applied across the full length of the spanning cell’s border.\n- Fixed a layout regression where absolutely positioned elements inside block-in-inline containers were incorrectly overlapping adjacent content.\n- Fixed a regression where content with `column-count: 1` could fail to display text. \n- Fixed an issue where `@font-face` rules with different styles could incorrectly fall back to glyphs from other faces in the same family, rather than proceeding to the next family as specified by the font matching algorithm.\n- Fixed `hanging-punctuation` to correctly treat U+0027 (apostrophe) and U+0022 (quotation mark) as hangable quote characters.\n- Fixed an issue where ideographic space did not hang when using `hanging-punctuation: first`.\n- Fixed an issue where media queries failed to re-evaluate during viewport resizing when CSS anchor positioning and viewport units were both in use.\n- Fixed an issue where chains of three or more anchor-positioned elements didn’t resolve correctly.\n- Fixed an issue where `lh` and `rlh` units resolved with double-zoom when `line-height` is a number.\n- Fixed an issue where the `rlh` unit was double-zoomed when resolving with evaluation-time zoom for unzoomed properties.\n- Fixed an issue where `anchor()` fallback values did not accept unitless zero.\n- Fixed an issue where an element with `display: contents` did not establish an anchor scope when using `anchor-scope`.\n- Fixed an issue where fixed-position boxes anchored to children of sticky-positioned boxes did not stick correctly.\n- Fixed an issue where `aspect-ratio` was not honored correctly when the page was zoomed in.\n- Fixed an issue where `:has(:empty)` was not invalidated when the content of a child element changed from empty to non-empty.\n\n### Editing\n\n#### Resolved Issues\n\n- Fixed an issue where pressing backspace on a line below an image in a `contenteditable` region could place the cursor in the wrong position.\n- Fixed an issue where emoji images copied from websites and pasted into other sites appeared broken due to cross-origin resource policy blocking the SVG image sources.\n- Fixed an issue where pasting text into an empty list item created an extra blank bullet.\n\n### Forms\n\n#### Resolved Issues\n\n- Fixed an issue where a `readonly` date `<input>` could still be edited via keyboard using the date picker.\n- Fixed an issue on iOS and iPadOS where `datalist` suggestions were presented directly over the associated input, obscuring it.\n\n### HTML\n\n#### Resolved Issues\n\n- Fixed `dragenter` and `dragleave` events to include `relatedTarget` in the event object.\n- Fixed an issue on iOS where the drag thumbnail could show an incorrect image after long-pressing an image with an embedded link.\n\n### Images\n\n#### Resolved Issues\n\n- Fixed a regression where images with `srcset` and `sizes` attributes containing `calc()` expressions with division by zero were not displayed.\n\n### JavaScript\n\n#### Resolved Issues\n\n- Fixed `TypedArray.prototype.sort()` failing when the comparison function accesses the `.buffer` property of the typed array.\n\n### Media\n\n#### Resolved Issues\n\n- Fixed an issue where the media controls volume button was mispositioned and overlapped with other controls in right-to-left locales.\n- Fixed an issue where `MediaCapabilities.decodingInfo()` always returned `false` for `spatialRendering`.\n\n### Networking\n\n#### Resolved Issues\n\n- Fixed an issue where downloaded files used the file extension from the URL path instead of the HTTP `Content-Type` header.\n- Fixed an issue where downloaded files were saved with incorrect or missing file extensions when the URL path extension did not match the HTTP `Content-Type` header.\n\n### Rendering\n\n#### Resolved Issues\n\n- Fixed an issue where content inside inline elements with block-level children and rendering layers was not displayed correctly.\n- Fixed an issue where `getClientRects()` could return rects with zero width and height for spans in multi-column layouts.\n- Fixed an issue where an empty `<span>` with decoration was incorrectly positioned when a sibling block margin was present inside a block-in-inline context.\n- Fixed an issue where a `<br>` element was incorrectly positioned inside a block-in-inline context when a block margin was present.\n- Fixed an issue where grid and flex layout could cause elements to shift position at certain zoom levels.\n- Fixed an issue where text content could get cut off inside overflow containers when the page was zoomed in.\n- Fixed an issue where images could appear stretched inside certain flex and grid layout configurations.\n- Fixed an issue where images inside transformed containers were not properly centered.\n- Fixed an issue where pinch-to-zoom could cause web content to jump or disappear on some websites.\n- Fixed an issue where user-installed font variants could interfere with system font matching, causing incorrect fonts to be selected.\n\n### SVG\n\n#### New Features\n\n- Added support for the `color-interpolation` attribute on SVG gradients, enabling `linearRGB` color space interpolation.\n\n#### Resolved Issues\n\n- Fixed an issue where `removeAttribute` for `width` or `height` on an SVG root element did not reset to the initial default values.\n- Fixed event name mapping for `onbegin`, `onend`, and `onrepeat` on `SVGAnimationElement` and added the missing `onend` event handler.\n- Fixed an issue where an SVG `<image>` element was not repainted when its `href` attribute was removed.\n- Fixed an issue where UI events such as wheel failed to fire for inner SVG elements.\n- Fixed an issue where SVG cursors set via `cursor: url()` appeared blurry on high DPI displays.\n\n### Scrolling\n\n#### Resolved Issues\n\n- Fixed an issue where scroll-snap re-snapping after layout changes could cause incorrect scroll positions, resulting in the wrong content being shown.\n\n### Storage\n\n#### Resolved Issues\n\n- Fixed an issue where IndexedDB connections could become permanently broken until the page was reloaded.\n- Fixed an issue where `document.hasStorageAccess()` could return a Promise that never resolves.\n\n### Web API\n\n#### New Features\n\n- Added support for the `source` property on `ToggleEvent`, which identifies the element that triggered the toggling of a target element such as a popover’s invoker button.\n- Added support for the Origin API, which exposes origin information as a structured `Origin` object.\n\n#### Resolved Issues\n\n- Fixed an issue where `DecompressionStream` discarded valid decompressed output when extra trailing bytes were present after the compressed stream, instead of enqueuing the output before throwing.\n- Fixed an issue where calling preventDefault() on pointerdown events did not prevent page scrolling when only passive touch event listeners are installed.\n\n### Web Extensions\n\n#### Resolved Issues\n\n- Fixed an issue where extensions with a trailing comma in `manifest.json` failed to load in Safari.\n\n### WebGL\n\n#### Resolved Issues\n\n- Fixed WebGL shader compilation to properly handle `NaN` and `infinity` values. \n\n### WebRTC\n\n#### Resolved Issues\n\n- Fixed `RTCIceCandidate.toJSON()` to include the `usernameFragment` property in its serialized output.\n- Fixed `RTCRtpSender` to allow a `maxFramerate` value of 0.\n- Fixed `RTCRtpSynchronizationSource.timestamp` to use the correct time base.\n- Fixed an issue where remote audio and video track IDs were incorrectly derived from SDP.\n- Fixed `RTCRtpTransceiver.setCodecPreferences()` to accept codecs with case-insensitive `mimeType` matching.\n- Fixed an issue where the camera did not turn on automatically in Google Meet when media permissions were set to “Allow”."
    },
    {
      "title": "Safari 26.6 Release Notes",
      "url": "https://developer.apple.com/documentation/safari-release-notes/safari-26_6-release-notes",
      "publishedAt": "2026-07-27T00:00:00.000Z",
      "versions": [
        "26.6"
      ],
      "text": "## Overview\n\nSafari 26.6 is available for iOS 26.6, iPadOS 26.6, visionOS 26.6, macOS 26.6, macOS Sequoia, and macOS Sonoma.\n\n### CSS\n\n#### Resolved Issues\n\n- Fixed an issue where the `ic` length unit scaled incorrectly with page zoom, causing it to no longer equal `1em` as expected by the CSS specification.\n- Fixed an issue where fixed-positioned elements using `position-area` did not fall back properly when the body was scrollable.\n- Fixed an issue where CSS `zoom` interacted incorrectly with `font-size`, `font-weight`, `font-variant`, and `font-style` on iPad when requesting the desktop website.\n\n### Networking\n\n#### Resolved Issues\n\n- Fixed an issue where partitioned cookies could not be deleted using `WKHTTPCookieStore`.\n\n### Service Workers\n\n#### Resolved Issues\n\n- Fixed an issue where service worker registrations with missing main scripts were not automatically unregistered, preventing pages from re-registering new service workers.\n- Fixed an issue where service worker registrations with missing imported scripts were not automatically unregistered.\n\n### Web Extensions\n\n#### Resolved Issues\n\n- Fixed an issue where web extension service worker registration database files accumulated on each Safari launch, causing performance degradation.\n\n### WebAssembly\n\n#### New Features\n\n- Added `compileOptions` parameter to `WebAssembly.compileStreaming` and `WebAssembly.instantiateStreaming` for JS String Builtins support.\n\n### WebRTC\n\n#### Resolved Issues\n\n- Fixed an issue where `RTCPeerConnection` configured with `iceTransportPolicy: \"relay\"` failed to gather any ICE candidates on macOS Sequoia."
    },
    {
      "title": "Safari 27 Release Notes",
      "url": "https://developer.apple.com/documentation/safari-release-notes/safari-27-release-notes",
      "publishedAt": "2026-09-14T00:00:00.000Z",
      "versions": [],
      "text": "## Overview\n\nSafari 27 is available for iOS 27, iPadOS 27, visionOS 27, macOS 27, macOS 26, and macOS Sequoia.\n\n### Accessibility\n\n#### New Features\n\n- Added support for the `ariaNotify` API, allowing web developers to programmatically make screen reader announcements.\n\n#### Resolved Issues\n\n（32 件は長いため省略）\n\n### Animations\n\n#### New Features\n\n- Added the `animation` property to `AnimationEvent` and `TransitionEvent` interfaces.\n\n#### Resolved Issues\n\n（3 件は長いため省略）\n\n### CSS\n\n#### New Features\n\n- Added support for `contain: style` applying to CSS quote counters, as specified in CSS Containment Level 2.\n- Added support for the case-sensitive modifier `s` in CSS attribute selectors.\n- Added support for the `stretch` keyword in box sizing properties.\n- Added support for the `:host:has()` compound selector in CSS.\n- Added support for the `:heading` pseudo-class.\n- Added support for the `revert-rule` keyword, which rolls back the cascade to behave as if the current style rule had not been present.\n- Added support for the `normal` and `none` values on the `position-anchor` CSS property.\n- Added support for `<image>` values in `light-dark()`.\n- Added support for the `insert` keyword for the `text-autospace` property.\n- Added support for transform-aware anchor positioning.\n- Added support for `image(<color>)` in the `<image>` value type.\n- Added support for `color-mix()` with more than two colors.\n- Added support for the `alpha()` relative color function.\n- Added support for forwarding missing color components when interpolating between analogous color spaces.\n- Added support for the `no-clamp` option on the CSS `progress()` function.\n\n#### Resolved Issues\n\n（198 件は長いため省略）\n\n### Canvas\n\n#### New Features\n\n- Made the radii argument of `CanvasPath.roundRect()` optional.\n\n#### Resolved Issues\n\n（3 件は長いため省略）\n\n### Editing\n\n#### New Features\n\n- Added menu items to convert editable text between Simplified and Traditional Chinese characters in the “Transformations” submenu.\n\n#### Resolved Issues\n\n（19 件は長いため省略）\n\n### Encoding\n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### Fonts\n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### Forms\n\n#### New Features\n\n- Added the customizable `<select>` element, allowing custom styling with `appearance: base-select` and custom content with the `<selectedcontent>` HTML element.\n\n#### Resolved Issues\n\n（21 件は長いため省略）\n\n### HTML\n\n#### New Features\n\n- Added support for the `auto` keyword in the `sizes` attribute on `<img>` elements, enabling automatic size calculation based on the rendered layout width.\n- Added support for the `shadowrootslotassignment` attribute on declarative shadow roots.\n- Added support for `tabindex`, `focus()`, `blur()`, and `autofocus` on MathML elements per the HTML Standard.\n- Added support for SVG `<a>` as an argument to `Origin.from()`.\n\n#### Resolved Issues\n\n（39 件は長いため省略）\n\n#### Deprecations\n\n- Deprecated the `href` attribute on all MathML elements except `<a>`.\n\n### Images\n\n#### New Features\n\n- Added support for applying HDR gain maps to images decoded into accelerated backing stores in the GPU process.\n\n#### Resolved Issues\n\n（10 件は長いため省略）\n\n### JavaScript\n\n#### New Features\n\n- Added support for the TC39 BigInt Math proposal, exposing `Math`-equivalent methods on `BigInt` such as `BigInt.pow` and `BigInt.sqrt`.\n- Added support for static `import defer` semantics.\n\n#### Resolved Issues\n\n（33 件は長いため省略）\n\n### MathML\n\n#### New Features\n\n- Added support for multiple-character operators in MathML.\n- Added support for embellished operator detection through `mrow` for underover layout.\n- Added support for operator dictionary entries for multi-character operators to align with the MathML Core specification.\n\n#### Resolved Issues\n\n（16 件は長いため省略）\n\n### Media\n\n#### New Features\n\n- Added support for setting `TextTrackCue.endTime` to `Infinity` to represent an unbounded cue duration.\n- Added support for synchronized video playback on displays using genlock on macOS. (311815@main, 312166@main) \n- Added support for overriding color space for hardware video decoders in WebCodecs.\n- Added support for mapping `nextslide` and `previousslide` MediaSession actions to `nexttrack` and `previoustrack` platform commands, so pressing next/previous track invokes a registered slide handler when no direct track handler is registered.\n\n#### Resolved Issues\n\n（61 件は長いため省略）\n\n### Model Element\n\n#### Resolved Issues\n\n（2 件は長いため省略）\n\n### Navigation\n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### Networking\n\n#### New Features\n\n- Added support for secure cookies on loopback hosts.\n\n#### Resolved Issues\n\n（17 件は長いため省略）\n\n### PDF\n\n#### Known Issues\n\n（1 件は長いため省略）\n\n#### Resolved Issues\n\n（3 件は長いため省略）\n\n### Performance\n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### Printing\n\n#### Resolved Issues\n\n（3 件は長いため省略）\n\n### Rendering\n\n#### New Features\n\n- Added support for subpixel inline layout, enabling more precise text and inline element positioning.\n- Added support for `anchor-valid` and `anchor-visible` as aliases of `anchors-valid` and `anchors-visible` in `position-visibility`.\n- Added support for the Dutch IJ digraph in `text-transform: capitalize` and `::first-letter`, correctly titlecasing “ij” to “IJ” at word starts when the content language is Dutch.\n\n#### Resolved Issues\n\n（91 件は長いため省略）\n\n### SVG\n\n#### New Features\n\n- Added support for the `lang` and `xml:lang` attribute in SVG.\n- Made SVG `<use>` work without specifying a fragment identifier.\n\n#### Resolved Issues\n\n（66 件は長いため省略）\n\n#### Deprecations\n\n- Removed the `SVGLocatable` and `SVGTransformable` interfaces to align with the SVG2 specification.\n- Removed the non-standard `nearestViewportElement` and `farthestViewportElement` properties from `SVGGraphicsElement`, aligning with the SVG2 specification.\n- Removed the `viewTarget` property from `SVGViewSpec` to align with the SVG2 specification.\n- Removed support for the non-standard `glyph-orientation-horizontal` property.\n\n### Scrolling\n\n#### New Features\n\n- Enabled scroll anchoring, which prevents visible jumps in scroll position when content is inserted or removed above the viewport.\n\n#### Resolved Issues\n\n（30 件は長いため省略）\n\n### Security\n\n#### Resolved Issues\n\n（8 件は長いため省略）\n\n### Spatial Web\n\n#### New Features\n\n- Added support for the `<model>` element on iOS, iPadOS, and macOS. \n- Added support for the `<img> controls` attribute on spatial and panorama photos.\n- Added support for immersive website environments in visionOS.\n- Added support for `dynamic-range-limit` on the `<model>` element.\n- Added support for tone mapping in the `<model>` element.\n- Added support for texture array projection layers in WebXR Layers.\n\n#### Resolved Issues\n\n（8 件は長いため省略）\n\n#### Deprecations\n\n- Removed the Spatial Backdrop developer preview in favor of the Immersive API.\n\n### Storage\n\n#### New Features\n\n- Added support for setting `maxAge` in the Cookie Store API via `cookieStore.set()`.\n\n#### Resolved Issues\n\n（3 件は長いため省略）\n\n### Tables\n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### Text\n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### UI Events\n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### Web API\n\n#### New Features\n\n- Added `WKWebView` API to enable or disable sending the Global Privacy Control (GPC) HTTP header for outgoing requests.\n- Added support for the service worker static routing API. \n- Added support for async iteration over `ReadableStream` objects using `for await...of` loops.\n- Added `srgb-linear` and `display-p3-linear` to `PredefinedColorSpace`.\n- Added support for `ReadableStream.from()` to create a `ReadableStream` from an async iterable or iterable.\n- Added support for transferring `ReadableStream` objects via `postMessage()`.\n\n#### Resolved Issues\n\n（84 件は長いため省略）\n\n### Web Audio\n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### Web Extensions\n\n#### New Features\n\n- Added support for the `tabId` key in `chrome.windows.create()`. \n- Added support for `runtime.getDocumentId()` web extension API.\n- Added reporting of uncaught JavaScript exceptions and unhandled promise rejections in Web Extension scripts.\n- Added support for propagating user gestures through `sendMessage()`, `connect()`, `postMessage()`, and `executeScript()` APIs, enabling extensions to perform gesture-requiring actions like media playback.\n\n#### Resolved Issues\n\n（5 件は長いため省略）\n\n### Web Inspector\n\n#### New Features\n\n- Added color contrast information within the Color Picker in Web Inspector.\n- Added support to show each individual request when there are redirects in the Network tab.\n- Improved discoverability of color formats and gamuts in the color picker by adding explicit format and gamut toggle controls.\n- Added support for showing contrast information in the Color Picker when editing background color properties.\n- Added Subgrid and Grid-Lanes badges to the Elements tab for easier identification of subgrid and grid-lanes layout contexts.\n- Added the layout root element to Layout event details in the Timeline tab.\n- Added unique colors for style events such as “Style Invalidated” and “Style Recalculated” in the Timeline view to distinguish them from layout events.\n- Added a separate column to show the node associated with layout and rendering events.\n\n#### Resolved Issues\n\n（50 件は長いため省略）\n\n### Web Views\n\n#### New Features\n\n- Added `WKSerializedNode` as a public API, enabling applications to clone DOM nodes including shadow roots between different `WKWebView` instances.\n- Added `WKJSHandle` as a public API, enabling applications to create and manipulate JavaScript object references across content worlds.\n- Added `WKContentWorldConfiguration` as a public API for configuring content world properties such as autofill scripting, shadow root access, and inspectability when creating a `WKContentWorld`.\n- Added `alternateRequest` and `overrideReferrerForAllRequests` properties to `WKWebpagePreferences` as public API, allowing applications to modify the main resource request during navigation and apply custom referrer headers across all resource loads.\n- Added a `willSubmitForm` callback to `WKNavigationDelegate` with a new `WKFormInfo` object, enabling applications to be notified of and respond to HTML form submissions. \n- Added `mainFrameNavigation` to `WKNavigationAction` and `navigation` to `WKNavigationResponse` as public API, allowing applications to reliably correlate navigation actions and responses with their originating loads.\n- Added `loadURL:` to `WKWebView` as a convenience method for loading a URL directly without wrapping it in an `NSURLRequest`.\n- Added `getCookiesForURL:` to `WKHTTPCookieStore` as public API, allowing applications to efficiently retrieve cookies matching a specific URL instead of fetching the entire cookie store.\n- Added the ability to clear a content world via `WKWebView`.\n\n### WebAssembly\n\n#### New Features\n\n- Added support for WebAssembly JavaScript Promise Integration (JSPI).\n\n#### Resolved Issues\n\n（3 件は長いため省略）\n\n### WebDriver\n\n#### New Features\n\n- Added WebDriver support for the Digital Credentials API, including commands to simulate wallet payloads, indefinite waits, and user rejection.\n- Allow your agent to connect to a Safari browser for development and debugging via the Safari MCP server. \n\n#### Resolved Issues\n\n（1 件は長いため省略）\n\n### WebGL\n\n#### Resolved Issues\n\n（4 件は長いため省略）\n\n### WebGPU\n\n#### New Features\n\n- Added support for the `clip_distances` builtin value in WGSL shaders.\n\n#### Resolved Issues\n\n（6 件は長いため省略）\n\n### WebRTC\n\n#### New Features\n\n- Added support for the `targetLatency` attribute in WebRTC.\n- Added support for the `RTCRtpCodec` dictionary and related constructs.\n- Added support for `RTCRtpReceiver.jitterBufferTarget`.\n- Added support for video source `width` and `height` in RTC stats.\n\n#### Resolved Issues\n\n（17 件は長いため省略）"
    },
    {
      "title": "Safari 27.2 Beta Release Notes",
      "url": "https://developer.apple.com/documentation/safari-release-notes/safari-27_2-release-notes",
      "publishedAt": "2026-09-16T00:00:00.000Z",
      "versions": [
        "27.2"
      ],
      "text": "## Overview\n\nSafari 27.2 beta is available for iOS 27.2 beta, iPadOS 27.2 beta, visionOS 27.2 beta, macOS 27.2 beta, macOS 26, and macOS Sequoia.\n\n### Accessibility\n\n#### Resolved Issues\n\n- Fixed list, table, tree, and canvas content being omitted from the accessible name or description when the containing element is referenced by `aria-labelledby` or `aria-describedby`.\n- Fixed VoiceOver repeating text it had already announced when a page streams content by re-rendering an `aria-live` region instead of appending to it.\n\n### Animations\n\n#### Resolved Issues\n\n- Fixed `animation-range-start` and `animation-range-end` resolving incorrectly when `zoom` is applied, including when the value is inherited from an element with a different zoom factor.\n\n### CSS\n\n#### Deprecations\n\n- Renamed the `flow-tolerance` property to `fit-tolerance` for `display: grid-lanes`, keeping `flow-tolerance` as a deprecated alias that Web Inspector now flags.\n\n### Forms\n\n#### New Features\n\n- Added support for `safe` and `unsafe` overflow alignment with `normal`, and for `safe` alignment on fixed position boxes so they stay in view even when that means overflowing their `position-area`. Also, applied `safe` alignment to `::picker(select)`, which keeps base appearance `<select>` pickers fully in view.\n\n#### Resolved Issues\n\n- Fixed: Updated the default styling for `appearance: base-select` to center-align items, use `lh` units for padding, and reset all font properties on ::picker-icon and ::checkmark.\n\n### HTML\n\n#### Resolved Issues\n\n- Fixed the window `load` event never being dispatched when a `readystatechange` handler starts a new load.\n\n### JavaScript\n\n#### Resolved Issues\n\n- Fixed `for await...of` over an async generator skipping the `Promise.prototype.constructor` lookup required by `PromiseResolve` when that property is replaced.\n\n### Media\n\n#### Resolved Issues\n\n- Fixed `AudioContext.decodeAudioData()` failing with an `EncodingError` for valid AAC and M4A files whose file brand is not `mp4`.\n- Fixed video showing a black screen while audio continued to play after the video decoder was invalidated.\n- Fixed `ManagedMediaSource` not firing the `startstreaming` event when seeking to a time that is not buffered, which could stall playback.\n\n### Networking\n\n#### Resolved Issues\n\n- Fixed cookies set by a top-level navigation’s own response being capped to a seven day lifetime when the site’s hostname is CNAMEd to a different registrable domain.\n\n### SVG\n\n#### Resolved Issues\n\n- Fixed dynamic changes to a `<foreignObject>` element’s `x` and `y` values having no effect.\n\n### Safari MCP\n\n#### Resolved Issues\n\n- Fixed Safari taking keyboard focus from other apps when creating tabs or capturing screenshots under Safari MCP control. \n\n### Web Extensions\n\n#### Resolved Issues\n\n- Fixed a significant delay before web extension content is injected when Safari is cold launched.\n\n### WebGPU\n\n#### Resolved Issues\n\n- Fixed a severe frame rate drop when calling `executeBundles()` with render bundles that contain `drawIndirect()` or `drawIndexedIndirect()`.\n\n### WebRTC\n\n#### Resolved Issues\n\n- Fixed an issue where `getUserMedia()` requests for audio permanently failed with `NotAllowedError` after the system audio service was reset."
    }
  ],
  "errors": []
}
